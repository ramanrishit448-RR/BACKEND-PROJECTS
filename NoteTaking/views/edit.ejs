<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Note</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body
    class="bg-gradient-to-br from-gray-900 via-black to-gray-800 min-h-screen flex items-center justify-center text-white"
  >
    <div
      class="w-11/12 sm:w-2/3 md:w-1/3 bg-white/10 backdrop-blur-xl p-8 rounded-2xl shadow-2xl border border-white/10"
    >
      <h1
        class="text-4xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500 text-center mb-2"
      >
        Edit Note
      </h1>
      <p class="text-gray-400 text-center text-sm mb-6">
        Update your note title below
      </p>

      <form action="/edit" method="POST" class="space-y-4">
        <input
          type="text"
          name="previous"
          readonly
          value="<%= filename %>"
          class="w-full px-4 py-3 rounded-lg bg-white/5 text-gray-400 border border-white/10 placeholder-gray-400 focus:outline-none cursor-not-allowed"
        />

        <input
          type="text"
          name="newName"
          placeholder="Enter new title..."
          class="w-full px-4 py-3 rounded-lg bg-white/5 text-white border border-white/10 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
        />

        <button
          type="submit"
          class="w-full bg-gradient-to-r from-blue-500 to-cyan-400 text-black font-semibold py-3 rounded-lg hover:scale-[1.02] hover:from-cyan-400 hover:to-blue-500 transition-all"
        >
          Update
        </button>
      </form>

      <div class="text-center mt-6">
        <a
          href="/"
          class="text-gray-400 hover:text-cyan-400 hover:underline transition"
        >
          ← Back to Home
        </a>
      </div>
    </div>
  </body>
</html>
