<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="main w-full min-h-screen">
      <div class="form w-1/3 mx-auto mt-10 text-white">
        <form action="/create" method="POST">
          <input
            type="text"
            placeholder="Title"
            class="border px-5 py-2 w-full rounded"
            name="title"
          />
          <textarea
            placeholder="Note"
            class="border px-5 py-2 mt-2 w-full rounded"
            name="note"
          ></textarea>
          <input
            type="submit"
            value="Add Note"
            class="bg-blue-400 text-white p-2 mt-2 rounded cursor-pointer"
          />
        </form>
      </div>
      <div class="notes w-1/3 mx-auto mt-10 text-white flex gap-3">
        <% if (files.length > 0) { %> <% files.forEach(function(file) { %>
        <div class="border p-5 rounded mb-5 bg-gray-700 relative">
          <h1 class="text-3xl text-white font-bold tracking-tighter">
            <%= file.split(".txt").join("") %>
          </h1>
          <a class="text-blue-500 inline-block mt-1" href="">Read more</a>
        </div>
        <% }); %> <% } else { %>
        <h2 class="text-2xl text-white">No Notes Available</h2>
        <% } %>
      </div>
      <h3 class="text-2xl text-white mt-1 hidden">Your Notes</h3>
    </div>
    <script src="/javascripts/script.js"></script>
  </body>
</html>
