<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Notes App</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <style>
      .glass-card {
        background: rgba(255, 255, 255, 0.06);
        backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.08);
        box-shadow: 0 4px 25px rgba(0, 0, 0, 0.5);
        transition: all 0.3s ease-in-out;
      }

      .glass-card:hover {
        transform: translateY(-6px) scale(1.02);
        box-shadow: 0 6px 25px rgba(0, 200, 255, 0.3);
        border-color: rgba(0, 200, 255, 0.3);
      }

      .input-glass {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #fff;
        transition: all 0.3s ease;
      }

      .input-glass:focus {
        border-color: #38bdf8;
        box-shadow: 0 0 10px #38bdf8;
        outline: none;
      }

      .btn-gradient {
        background: linear-gradient(90deg, #3b82f6, #06b6d4);
        transition: all 0.3s ease;
      }

      .btn-gradient:hover {
        background: linear-gradient(90deg, #facc15, #f97316);
        color: #000;
        transform: translateY(-2px);
      }

      .fade-in {
        animation: fadeIn 0.7s ease forwards;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>

  <body class="bg-black min-h-screen flex flex-col items-center">
    <h1
      class="text-4xl font-extrabold mt-10 text-transparent bg-clip-text"
      style="
        background-image: linear-gradient(90deg, #60a5fa, #22d3ee, #a78bfa);
      "
    >
      My Notes
    </h1>

    <div class="form w-full max-w-md mt-10 text-white fade-in">
      <form
        action="/create"
        method="POST"
        class="flex flex-col gap-3 p-6 rounded-xl glass-card"
      >
        <input
          type="text"
          placeholder="Title"
          class="px-5 py-3 w-full rounded-lg input-glass"
          name="title"
        />
        <textarea
          placeholder="Write your note..."
          class="px-5 py-3 w-full h-32 rounded-lg input-glass resize-none"
          name="note"
        ></textarea>
        <input
          type="submit"
          value="Add Note"
          class="btn-gradient text-white p-3 mt-2 rounded-lg cursor-pointer font-semibold"
        />
      </form>
    </div>

    <div
      class="notes w-full max-w-3xl mt-10 text-white flex flex-wrap gap-5 justify-center"
    >
      <% if (files.length > 0) { %> <% files.forEach(function(file) { %>
      <div class="glass-card p-5 rounded-xl fade-in w-72 relative group">
        <h2 class="text-2xl font-bold mb-2 text-blue-300 tracking-tight">
          <%= file.split(".txt").join("") %>
        </h2>
        <div class="flex justify-between items-center">
          <a href="/file/<%= file %>" class="text-cyan-400 hover:underline"
            >Read</a
          >
          <a href="/edit/<%= file %>" class="text-yellow-400 hover:underline"
            >Edit</a
          >
        </div>
        <div
          class="relative inset-0 bg-cyan-400/0 group-hover:bg-cyan-400/10 rounded-xl transition"
        ></div>
      </div>
      <% }); %> <% } else { %>
      <h2 class="text-2xl text-gray-400 mt-10">No Notes Available</h2>
      <% } %>
    </div>

    <script src="/javascripts/script.js"></script>
  </body>
</html>
