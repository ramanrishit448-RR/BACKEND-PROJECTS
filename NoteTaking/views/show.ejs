<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View Note</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body
    class="bg-gradient-to-br from-gray-900 via-black to-gray-800 min-h-screen flex flex-col items-center justify-center text-white"
  >
    <div
      class="w-11/12 sm:w-2/3 md:w-1/2 bg-white/10 backdrop-blur-xl p-10 rounded-3xl shadow-2xl border border-white/10"
    >
      <h1
        class="text-4xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 to-orange-500 text-center mb-6"
      >
        <%= filename.split(".txt").join("") %>
      </h1>

      <p
        class="text-gray-300 text-lg leading-relaxed bg-white/5 p-5 rounded-xl border border-white/10 shadow-inner"
      >
        <%= data %>
      </p>

      <div class="mt-8 text-center">
        <a
          href="/"
          class="inline-block bg-gradient-to-r from-yellow-400 to-orange-400 text-black font-semibold px-6 py-3 rounded-xl hover:scale-105 transition-transform duration-200"
        >
          ← Back to Home
        </a>
      </div>
    </div>
  </body>
</html>
