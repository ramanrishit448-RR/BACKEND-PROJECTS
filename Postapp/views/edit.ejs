<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit Post</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">
      <div class="max-w-xl mx-auto p-6">
        <h1 class="text-3xl font-bold mb-4">Edit Post</h1>
        <form
          action="/post/<%= post._id %>/edit"
          method="post"
          class="flex flex-col items-center"
        >
          <input
            type="text"
            name="title"
            placeholder="Title"
            value="<%= post.title %>"
            class="w-full border-2 border-black p-2 m-2 rounded-md bg-zinc-800"
          />
          <textarea
            name="content"
            placeholder="Content"
            class="w-full border-2 border-black p-2 m-2 rounded-md bg-zinc-800"
          >
<%= post.content %></textarea
          >
          <div class="flex w-full justify-between">
            <a
              href="/profile"
              class="px-4 py-2 m-2 rounded-md bg-zinc-700 hover:bg-zinc-600"
              >Cancel</a
            >
            <button
              type="submit"
              class="px-4 py-2 m-2 rounded-md bg-blue-500 hover:bg-blue-600"
            >
              Save
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
