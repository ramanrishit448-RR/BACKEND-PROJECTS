<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Login</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap");

      body {
        font-family: "Inter", sans-serif;
      }

      .form-container {
        background: linear-gradient(
          145deg,
          rgba(255, 255, 255, 0.08) 0%,
          rgba(255, 255, 255, 0.02) 100%
        );
        backdrop-filter: blur(12px);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3),
          inset 0 1px 0 rgba(255, 255, 255, 0.1),
          inset 0 -1px 0 rgba(0, 0, 0, 0.2);
      }

      .input-field {
        background: rgba(24, 24, 27, 0.7);
        transition: all 0.3s ease;
      }

      .input-field:focus {
        background: rgba(24, 24, 27, 0.9);
        box-shadow: 0 0 0 3px rgba(250, 204, 21, 0.2);
      }

      .login-btn {
        background: linear-gradient(135deg, #facc15 0%, #eab308 100%);
        box-shadow: 0 4px 15px rgba(250, 204, 21, 0.3);
        transition: all 0.3s ease;
      }

      .login-btn:hover {
        background: linear-gradient(135deg, #fde047 0%, #facc15 100%);
        box-shadow: 0 6px 20px rgba(250, 204, 21, 0.4);
        transform: translateY(-2px);
      }

      .login-btn:active {
        transform: translateY(0);
      }

      .divider {
        display: flex;
        align-items: center;
        text-align: center;
        color: rgba(255, 255, 255, 0.4);
      }

      .divider::before,
      .divider::after {
        content: "";
        flex: 1;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }

      .divider::before {
        margin-right: 0.75em;
      }

      .divider::after {
        margin-left: 0.75em;
      }

      .social-btn {
        background: rgba(39, 39, 42, 0.7);
        border: 1px solid rgba(255, 255, 255, 0.1);
        transition: all 0.3s ease;
      }

      .social-btn:hover {
        background: rgba(39, 39, 42, 0.9);
        border-color: rgba(250, 204, 21, 0.4);
        transform: translateY(-1px);
      }

      .pulse {
        animation: pulse 2s infinite;
      }

      @keyframes pulse {
        0% {
          box-shadow: 0 0 0 0 rgba(250, 204, 21, 0.4);
        }
        70% {
          box-shadow: 0 0 0 10px rgba(250, 204, 21, 0);
        }
        100% {
          box-shadow: 0 0 0 0 rgba(250, 204, 21, 0);
        }
      }

      .floating-label {
        position: absolute;
        top: 50%;
        left: 1rem;
        transform: translateY(-50%);
        color: rgba(255, 255, 255, 0.6);
        pointer-events: none;
        transition: all 0.3s ease;
      }

      .input-field:focus + .floating-label,
      .input-field:not(:placeholder-shown) + .floating-label {
        top: 0;
        left: 0.75rem;
        font-size: 0.75rem;
        background: rgba(24, 24, 27, 0.9);
        padding: 0 0.5rem;
        color: #facc15;
      }
    </style>
  </head>
  <body
    class="min-h-screen flex items-center justify-center bg-gradient-to-br from-zinc-950 via-zinc-900 to-zinc-800 text-white p-4"
  >
    <div
      class="form-container w-full max-w-md p-8 rounded-2xl border border-white/10"
    >
      <div class="text-center mb-8">
        <div
          class="w-16 h-16 bg-gradient-to-br from-yellow-400 to-yellow-600 rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg"
        >
          <i class="fas fa-lock text-xl text-zinc-900"></i>
        </div>
        <h1 class="text-3xl font-bold tracking-tight">Welcome Back</h1>
        <p class="text-gray-400 mt-2">Sign in to your account to continue</p>
      </div>

      <form action="/login" method="POST" class="flex flex-col gap-5">
        <div class="relative">
          <input
            type="text"
            name="username"
            placeholder=" "
            required
            class="input-field w-full p-4 rounded-xl text-white border border-white/10 focus:outline-none focus:border-yellow-400"
          />
          <label class="floating-label">Username</label>
          <div
            class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400"
          >
            <i class="fas fa-user"></i>
          </div>
        </div>

        <div class="relative">
          <input
            type="password"
            name="password"
            placeholder=" "
            required
            class="input-field w-full p-4 rounded-xl text-white border border-white/10 focus:outline-none focus:border-yellow-400"
          />
          <label class="floating-label">Password</label>
          <div
            class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400"
          >
            <i class="fas fa-lock"></i>
          </div>
        </div>

        <div class="flex justify-between items-center text-sm">
          <label class="flex items-center text-gray-400">
            <input
              type="checkbox"
              class="rounded bg-zinc-800 border-gray-600 text-yellow-400 focus:ring-yellow-400 mr-2"
            />
            Remember me
          </label>
          <a
            href="#"
            class="text-yellow-400 hover:text-yellow-300 transition-colors"
            >Forgot password?</a
          >
        </div>

        <button
          type="submit"
          class="login-btn w-full py-4 mt-2 text-black font-semibold rounded-xl transition-all duration-300 pulse"
        >
          <span class="flex items-center justify-center">
            <i class="fas fa-sign-in-alt mr-2"></i>
            Sign In
          </span>
        </button>
      </form>

      <div class="divider my-6">or continue with</div>

      <div class="grid grid-cols-2 gap-3 mb-6">
        <button
          class="social-btn py-3 rounded-xl flex items-center justify-center"
        >
          <i class="fab fa-google text-red-400 mr-2"></i>
          <span>Google</span>
        </button>
        <button
          class="social-btn py-3 rounded-xl flex items-center justify-center"
        >
          <i class="fab fa-github text-gray-300 mr-2"></i>
          <span>GitHub</span>
        </button>
      </div>

      <div class="text-center text-gray-400 text-sm">
        <p>
          Don't have an account?
          <a
            href="/register"
            class="text-yellow-400 hover:text-yellow-300 font-medium transition-colors ml-1"
            >Create account</a
          >
        </p>
      </div>
    </div>
  </body>
</html>
